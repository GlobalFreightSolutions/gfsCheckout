import eachDayOfInterval from"../eachDayOfInterval/index.js";import isSunday from"../isSunday/index.js";import isWeekend from"../isWeekend/index.js";export default function eachWeekendOfInterval(interval){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var dateInterval=eachDayOfInterval(interval),weekends=[],index=0;while(index++<dateInterval.length){var date=dateInterval[index];if(isWeekend(date)){weekends.push(date);if(isSunday(date))index=index+5}}return weekends}