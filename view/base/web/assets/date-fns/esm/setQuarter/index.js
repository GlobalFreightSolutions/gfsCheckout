import toInteger from"../_lib/toInteger/index.js";import toDate from"../toDate/index.js";import setMonth from"../setMonth/index.js";export default function setQuarter(dirtyDate,dirtyQuarter){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),quarter=toInteger(dirtyQuarter),oldQuarter=Math.floor(date.getMonth()/3)+1;return setMonth(date,date.getMonth()+3*(quarter-oldQuarter))}