import toDate from"../toDate/index.js";export default function startOfMonth(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate);date.setDate(1);date.setHours(0,0,0,0);return date}