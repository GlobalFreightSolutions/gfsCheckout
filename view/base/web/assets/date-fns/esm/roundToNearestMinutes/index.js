import toDate from"../toDate/index.js";import toInteger from"../_lib/toInteger/index.js";export default function roundToNearestMinutes(dirtyDate,options){if(1>arguments.length){throw new TypeError("1 argument required, but only none provided present")}var nearestTo=options&&"nearestTo"in options?toInteger(options.nearestTo):1;if(1>nearestTo||30<nearestTo){throw new RangeError("`options.nearestTo` must be between 1 and 30")}var date=toDate(dirtyDate),seconds=date.getSeconds(),minutes=date.getMinutes()+seconds/60,roundedMinutes=Math.floor(minutes/nearestTo)*nearestTo,addedMinutes=Math.round(minutes%nearestTo/nearestTo)*nearestTo;return new Date(date.getFullYear(),date.getMonth(),date.getDate(),date.getHours(),roundedMinutes+addedMinutes)}