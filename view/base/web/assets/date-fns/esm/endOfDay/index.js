import toDate from"../toDate/index.js";export default function endOfDay(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate);date.setHours(23,59,59,999);return date}