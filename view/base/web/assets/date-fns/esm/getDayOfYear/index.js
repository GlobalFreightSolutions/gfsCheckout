import toDate from"../toDate/index.js";import startOfYear from"../startOfYear/index.js";import differenceInCalendarDays from"../differenceInCalendarDays/index.js";export default function getDayOfYear(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),diff=differenceInCalendarDays(date,startOfYear(date));return diff+1}