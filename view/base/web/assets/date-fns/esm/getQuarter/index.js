import toDate from"../toDate/index.js";export default function getQuarter(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),quarter=Math.floor(date.getMonth()/3)+1;return quarter}