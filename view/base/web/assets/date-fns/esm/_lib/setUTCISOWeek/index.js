import toInteger from"../toInteger/index.js";import toDate from"../../toDate/index.js";import getUTCISOWeek from"../getUTCISOWeek/index.js";export default function setUTCISOWeek(dirtyDate,dirtyISOWeek){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),isoWeek=toInteger(dirtyISOWeek),diff=getUTCISOWeek(date)-isoWeek;date.setUTCDate(date.getUTCDate()-7*diff);return date}