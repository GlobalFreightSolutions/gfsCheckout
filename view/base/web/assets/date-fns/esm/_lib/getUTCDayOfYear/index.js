import toDate from"../../toDate/index.js";var MILLISECONDS_IN_DAY=864e5;export default function getUTCDayOfYear(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),timestamp=date.getTime();date.setUTCMonth(0,1);date.setUTCHours(0,0,0,0);var startOfYearTimestamp=date.getTime();return Math.floor((timestamp-startOfYearTimestamp)/MILLISECONDS_IN_DAY)+1}