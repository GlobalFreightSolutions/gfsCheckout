import toInteger from"../toInteger/index.js";import toDate from"../../toDate/index.js";import getUTCWeek from"../getUTCWeek/index.js";export default function setUTCWeek(dirtyDate,dirtyWeek,options){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),week=toInteger(dirtyWeek),diff=getUTCWeek(date,options)-week;date.setUTCDate(date.getUTCDate()-7*diff);return date}