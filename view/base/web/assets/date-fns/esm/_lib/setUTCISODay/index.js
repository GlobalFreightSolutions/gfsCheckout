import toInteger from"../toInteger/index.js";import toDate from"../../toDate/index.js";export default function setUTCISODay(dirtyDate,dirtyDay){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var day=toInteger(dirtyDay);if(0===day%7){day=day-7}var date=toDate(dirtyDate),currentDay=date.getUTCDay(),remainder=day%7,diff=((remainder+7)%7<1?7:0)+day-currentDay;date.setUTCDate(date.getUTCDate()+diff);return date}