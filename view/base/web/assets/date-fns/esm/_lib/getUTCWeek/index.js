import toDate from"../../toDate/index.js";import startOfUTCWeek from"../startOfUTCWeek/index.js";import startOfUTCWeekYear from"../startOfUTCWeekYear/index.js";var MILLISECONDS_IN_WEEK=6048e5;export default function getUTCWeek(dirtyDate,options){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),diff=startOfUTCWeek(date,options).getTime()-startOfUTCWeekYear(date,options).getTime();return Math.round(diff/MILLISECONDS_IN_WEEK)+1}