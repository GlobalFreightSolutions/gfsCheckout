import toInteger from"../_lib/toInteger/index.js";import toDate from"../toDate/index.js";import getISOWeek from"../getISOWeek/index.js";export default function setISOWeek(dirtyDate,dirtyISOWeek){if(2>arguments.length){throw new TypeError("2 arguments required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),isoWeek=toInteger(dirtyISOWeek),diff=getISOWeek(date)-isoWeek;date.setDate(date.getDate()-7*diff);return date}