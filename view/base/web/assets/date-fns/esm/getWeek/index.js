import toDate from"../toDate/index.js";import startOfWeek from"../startOfWeek/index.js";import startOfWeekYear from"../startOfWeekYear/index.js";var MILLISECONDS_IN_WEEK=6048e5;export default function getWeek(dirtyDate,options){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),diff=startOfWeek(date,options).getTime()-startOfWeekYear(date,options).getTime();return Math.round(diff/MILLISECONDS_IN_WEEK)+1}