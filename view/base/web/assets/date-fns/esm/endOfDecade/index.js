import toDate from"../toDate/index.js";export default function endOfDecade(dirtyDate){if(1>arguments.length){throw new TypeError("1 argument required, but only "+arguments.length+" present")}var date=toDate(dirtyDate),year=date.getFullYear(),decade=9+10*Math.floor(year/10);date.setFullYear(decade,11,31);date.setHours(23,59,59,999);return date}